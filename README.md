# 🚧 Remote Desktop & API Server 🚧

> **Проект в разработке**  
> Этот проект ещё развивается — функционал может меняться, но уже демонстрирует работу серверной части на C++20 и клиентской на Qt.

---

## 🖥 О проекте

**Remote Desktop** — современная система для удалённого управления устройствами с графическим интерфейсом на **Qt** и серверной частью на **C++20 с Boost.Beast**.

Проект состоит из двух компонентов:

### 1️⃣ API-сервер
- Реализован на **C++20** с использованием **Boost.Beast**
- Поддерживает **WebSocket** для двустороннего обмена данными
- Обрабатывает **регистрацию, авторизацию и команды** от клиентов
- Возможность расширения под работу с базой данных и безопасное шифрование (через **libsodium**)

### 2️⃣ Qt-клиент
- Интерфейс пользователя для **регистрации, авторизации и управления удалёнными устройствами**
- Подключение к серверу через **WebSocket**
- Кроссплатформенный: Linux, macOS, Windows
- Отправка команд и получение ответов в реальном времени

---

## ⚙️ Технологии
- **C++20** — современный язык для серверной логики
- **Boost.Beast** — WebSocket и HTTP сервер
- **Qt** — GUI и клиентская логика
- **libsodium** — криптография и безопасное хранение данных
- **PostgreSQL** — планируется для хранения пользователей и сессий

---

## 🚀 Возможности
- Регистрация и авторизация пользователей через Qt-приложение
- Двустороннее взаимодействие между клиентом и сервером через WebSocket
- Удалённое управление устройствами с одного приложения
- Масштабируемый сервер, возможность подключения множества клиентов

---

## 📌 Цель проекта
Создать современную, безопасную и кроссплатформенную систему удалённого управления устройствами, которая может использоваться для IoT, администрирования и обмена командами между устройствами.

---

## ⚡ Сборка на NixOS
1. Создать `shell.nix` с нужными зависимостями (Boost, libsodium, cmake, ninja)
2. Войти в dev-shell:

```bash
nix-shell
